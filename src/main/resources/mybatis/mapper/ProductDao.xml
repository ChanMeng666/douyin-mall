<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qxy.dao.mapper.ProductDao">
    <resultMap id="ProductResultMap" type="com.qxy.model.po.Product">
        <id column="product_id" property="productId" />
        <result column="name" property="name" />
        <result column="description" property="description" />
        <result column="price" property="price" />
        <result column="stock" property="stock" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="image_url" property="imageUrl" />
    </resultMap>

    <select id="selectAll" resultMap="ProductResultMap">
        select * from products
    </select>

    <select id="selectById" resultMap="ProductResultMap">
        select * from products where product_id = #{productId}
    </select>

    <select id="selectByIds" resultMap="ProductResultMap">
        select * from products
        where product_id in
        <foreach collection="productId" open="(" close=")" item="id" separator=",">
            #{id}
        </foreach>
    </select>
    <select id="queryProducts" resultType="com.qxy.model.po.Product" resultMap="ProductResultMap">
        select * from products
    </select>

    <update id="reduceProductStock">
        update products set stock = stock - #{quantity} 
        where product_id = #{productId} and stock >= #{quantity}
    </update>
</mapper>